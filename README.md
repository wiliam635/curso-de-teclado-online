# Curso de Teclado Online

Landing page reformulada para o curso de teclado do Wiliam Silva.

## Prévia local

Abra `index.html` no navegador ou execute:

```bash
python3 -m http.server 4173
```

## Estrutura

- `index.html` — página principal
- `site-assets/` — imagens utilizadas no layout
